# 🛡️ AI Core Directives: Smart IoT Attendance System

> **CRITICAL INSTRUCTION FOR AI AGENT:** 
> Whenever you are invoked in this workspace, you **MUST** read and adhere to these rules before writing a single line of code. Failure to do so violates the core project directive.

## 1. 🧠 Holistic Architecture First (Think Big Picture)
- **No Blind Edits:** Before adding a feature, modifying code, or fixing a bug, you must analyze how the change impacts the *entire* system (FastAPI Backend, React Frontend, Mobile App, ESP32 IoT Devices, and PostgreSQL Database).
- **Cross-Platform Consistency:** If a feature is added to the Website, you must consider or plan its integration into the Android Application immediately.
- **Zero Regression:** Ensure new changes do not break existing features. Every fix must be verified against the whole system pipeline.

## 2. ⚡ Skill Maximization & Professional Output
- **Explicit Skill Declaration:** At the beginning of every task, you must explicitly state which professional skills (e.g., `backend-architect`, `react-patterns`) you are loading to complete the work.
- **Mandatory Prompt Engineering:** You MUST always process the user's requests using the local `skills/prompt-engineering` directory. This ensures any request is automatically elevated to a highly professional standard and perfectly aligned with the entire project context before you begin coding.
- **Production-Ready Code Only:** Do not write "prototype", "lazy", or "temporary" code. All code must be robust, highly performant, and ready for a live production environment.
- **Defensive Engineering:** Anticipate edge cases. Implement strict error handling, retry logic, and timeouts—especially when dealing with hardware (ESP32) or local SLM models.

## 3. 🔒 Strict Offline & Hardware Constraints
- **100% Offline Architecture:** The system must run entirely on the local network via the Raspberry Pi 4. **Do not** integrate external cloud APIs (no Groq, no OpenAI, no cloud databases) under any circumstances unless explicitly commanded by the user.
- **Resource Efficiency:** Optimize all code to run smoothly within the hardware limitations of a Raspberry Pi 4. Ensure memory leaks are avoided and background tasks are properly managed.

## 4. 🧹 Technical Debt & Clean Code
- **Refactor as You Go:** If you see inefficient or deprecated code near your working area, clean it up. Keep the system modern.
- **Documentation:** Ensure all new logic is clearly commented and that system architecture documents (like `PROJECT_BRIEF.md` and the `DNA` folders) are updated when major changes occur.

---
*By reading this document, the AI Agent acknowledges the highest standard of professional engineering required for this project and commits to executing tasks accordingly.*
